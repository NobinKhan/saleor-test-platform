{
  "step_id": "06_list_after_delete",
  "name": "Confirm product deleted",
  "auth_context": "staff",
  "input_sent": "query ProductLifecycleGetDeleted($id: ID!) { product(id: $id) { id slug } }",
  "variables": {
    "id": "{{context.created_product_id}}"
  },
  "assertions": [
    {"kind": "equals", "path": "$.data.product", "expected": null}
  ]
}
