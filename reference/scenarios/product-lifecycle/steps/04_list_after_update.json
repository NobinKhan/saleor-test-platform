{
  "step_id": "04_list_after_update",
  "name": "Verify updated name by ID",
  "auth_context": "staff",
  "input_sent": "query ProductLifecycleGetUpdated($id: ID!) { product(id: $id) { id name slug } }",
  "variables": {
    "id": "{{context.created_product_id}}"
  },
  "assertions": [
    {"kind": "equals", "path": "$.data.product.name", "expected": "Harness Scenario Product Updated"}
  ]
}
